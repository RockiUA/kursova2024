import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    backgroundColor: '#EBEBEF',
    fontSize: 18,
  },
  inputOutline: {
    borderRadius: 12,
  },
  errorMessage: {
    color: '#D3232F',
  },
});
