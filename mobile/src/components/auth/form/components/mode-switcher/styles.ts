import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 15,
    borderColor: '#D8D8DF',
    borderWidth: 1,
    borderRadius: 8,
    justifyContent: 'center',
    flexDirection: 'row',
    gap: 10,
  },
  link: {
    color: '#054DA7',
    fontWeight: '700',
  },
});
