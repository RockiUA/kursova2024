import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: '5%',
  },
  input: {
    backgroundColor: '#DEDEE1',
    fontSize: 18,
    borderRadius: 12,
    paddingVertical: 0,
  },
});
