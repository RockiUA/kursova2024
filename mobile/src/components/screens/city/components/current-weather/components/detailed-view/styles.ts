import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 15,
  },
  cardContainer: {
    flexDirection: 'row',
    flex: 2,
    flexWrap: 'wrap',
    gap: 15,
    marginBottom: 15,
  },
});
