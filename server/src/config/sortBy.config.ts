import { SortByConfig } from './interfaces';

export const sortByConfig: SortByConfig = {
  city: ['id', 'name', 'longitude', 'latitude'],
  role: ['id', 'value', 'description'],
  user: ['id', 'email'],
};
